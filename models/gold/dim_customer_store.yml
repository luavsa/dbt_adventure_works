version: 2

models:
  - name: dim_customer_store
    description: >
      Tabela dimensão de lojas dos clientes da Adventure Works.
    columns:
      - name: pk_store
        description: chave primária da dimensão de lojas de clientes.
        tests:
          - unique
          - not_null
      - name: store
        description: nome da loja.