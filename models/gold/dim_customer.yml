version: 2

models:
  - name: dim_customer
    description: >
      Tabela dimensão dos clientes da Adventure Works.
    columns:
      - name: pk_customer
        description: Chave primária da dimensão de clientes.
        tests:
          - unique
          - not_null